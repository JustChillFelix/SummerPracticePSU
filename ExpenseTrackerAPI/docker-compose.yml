version: '3.8'
services:
  mongodb:
    image: mongo:7
    container_name: expense-tracker-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: expense_tracker

volumes:
  mongodb_data: